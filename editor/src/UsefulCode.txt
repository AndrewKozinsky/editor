import { useDispatch, useSelector } from 'react-redux'

const dispatch = useDispatch()

// StoreSettingsTypes.EditorLanguage

// Язык интерфейса
const lang = useSelector((store: AppStateType) => store.settings.editorLanguage)


$CN: left-part-3;

.#{$CN} {
    padding: 1.5rem 2rem;
}


.#{$CN}--small-size {
    font-size: 1.4rem;
    line-height: 1.8rem;
}