import './EditArticleSection.scss'

const rootClass = 'edit-article-section'

/** Функция возвращающая классы для элементов */
export default function makeClasses() {
    return {
        tip: `${rootClass}__tip`,
    }
}
