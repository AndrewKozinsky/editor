import SiteModel from '../models/site'

/**
 * Функция возвращает id сайта по id пользователя
 * @param {String} userId — id пользователя
 */
// ЭТУ ФУНКЦИЮ НУЖНО УДАЛИТЬ ПОТОМУ ЧТО ОНА БЕССМЫСЛЕННА
/*
export async function getSiteIdByUserId(userId: string): Promise<null | string> {
    const site = await SiteModel.findOne({userId})
    console.log(userId)


    return site ? site._id : null
}*/
